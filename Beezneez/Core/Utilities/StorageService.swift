import Foundation

// base code for saving and accessing things locally
// authentication itself is handled separately, but this can access keychain and such
// save local cache or offline users or user data

struct StorageService {
    // cache keys or time keys
    // persist onjects, with keys and extra info
    // excpire keys
    // set cache times
    // get cache times
    // clear storage
}
